import CreateProject from "@/screens/auth/CreateProject";
import { Page } from "@/types";

const AccountVerification: Page = () => {
  return <CreateProject />;
};

export default AccountVerification;
