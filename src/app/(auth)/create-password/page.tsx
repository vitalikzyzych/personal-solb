import CreatePassword from "@/screens/auth/CreatePassword";

import { Page } from "@/types";

const CreatePasswordPage: Page = () => {
  return <CreatePassword />;
};

export default CreatePasswordPage;
