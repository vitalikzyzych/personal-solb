import React from "react";
import type { Page } from "@/types";
import NotFound from "@/screens/helpers/NotFound";

const Custom404: Page = () => {
  return <NotFound />;
};

export default Custom404;
